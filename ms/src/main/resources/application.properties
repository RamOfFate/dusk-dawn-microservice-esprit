spring.application.name=Candidat4TWIN2Ms

# Config Server (overridden in Docker via CONFIG_SERVER_URL)
spring.config.import=optional:configserver:
spring.cloud.config.uri=${CONFIG_SERVER_URL:http://localhost:8888}
