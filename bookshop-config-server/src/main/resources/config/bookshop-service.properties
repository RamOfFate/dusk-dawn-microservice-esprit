server.port=8080

# H2 (file-based) datasource (persistent)
spring.datasource.url=jdbc:h2:file:./Database/Data/bookshopdb;AUTO_SERVER=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Optional: H2 console for local debugging
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# review-service integration (OpenFeign)
# Client name must match spring.application.name of review-service.
spring.cloud.openfeign.client.config.review-service.connectTimeout=2000
spring.cloud.openfeign.client.config.review-service.readTimeout=4000

# Optional logging (NONE | BASIC | HEADERS | FULL)
# spring.cloud.openfeign.client.config.review-service.loggerLevel=BASIC

eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
eureka.instance.prefer-ip-address=true

